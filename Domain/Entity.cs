namespace Domain
{
    // all classes in this namespace will serve as core domain objects
    // these will correspond to entities in database
    // these will derive from base entity class
    public class Entity : BaseEntity
    {
    }
}
